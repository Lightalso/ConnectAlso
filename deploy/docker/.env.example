# ConnectAlso 环境变量配置
# 复制为 .env 后修改

# 日志级别: trace, debug, info, warn, error
RUST_LOG=info

# ── 服务端口 ──
CONTROL_PORT=3000
RELAY_PORT=33478
STUN_PORT=3478

# ── 控制服务配置 ──
# 虚拟网络 CIDR (分配给设备的 IPv4 地址池)
CONTROL_NETWORK=100.64.0.0/16

# 设备心跳超时（秒），超时未心跳自动注销
STALE_TIMEOUT=300
